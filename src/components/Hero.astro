<div class="bienvenida"></div>
<section id="hero">
    <div id="hero-content">
        <h1 style="text-align: center; line-height: 1;">
            🎯 ¡Empieza tu transformación con FitMe!
        </h1>
        <p>Tu camino hacia el mejor estado físico a un solo click 🖱️</p>
        <button id="button-generator">
            <span>Ir al creador</span>
             <svg
                width="20"
                height="auto"
                viewBox="0 0 49 49"
                fill="none"
                xmlns="http://www.w3.org/2000/svg"
            >
                <path
                    d="M24.5 13.25C24.5 19.4632 19.4632 24.5 13.25 24.5H2V13.25C2 7.03679 7.03679 2 13.25 2C19.4632 2 24.5 7.03679 24.5 13.25Z"
                    fill="#FFFFFF"></path>
                <path
                    d="M24.5 35.75C24.5 29.5368 29.5368 24.5 35.75 24.5H47V35.75C47 41.9632 41.9632 47 35.75 47C29.5368 47 24.5 41.9632 24.5 35.75Z"
                    fill="#FFFFFF"></path>
                <path
                    d="M2 35.75C2 41.9632 7.03679 47 13.25 47H24.5V35.75C24.5 29.5368 19.4632 24.5 13.25 24.5C7.03679 24.5 2 29.5368 2 35.75Z"
                    fill="#FFFFFF"></path>
                <path
                    d="M47 13.25C47 7.03679 41.9632 2 35.75 2H24.5V13.25C24.5 19.4632 29.5368 24.5 35.75 24.5C41.9632 24.5 47 19.4632 47 13.25Z"
                    fill="#FFFFFF"></path>
            </svg>
        </button>
    </div>
</section>

<style>
    @import "/src/assets/font/font.css";
    @import "../styles/global.css";

    #hero {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100%;
        background-repeat: no-repeat;
        background-size: cover;
        background-position: center;
        transition: background 0.3s ease;
    }

    #hero-content {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        padding: 3rem;
        border-radius: 20px;
        background: rgba(
            255,
            255,
            255,
            0.1
        ); /* Más transparente para efecto vidrio */
        backdrop-filter: blur(12px) saturate(180%);
        -webkit-backdrop-filter: blur(12px) saturate(180%);
        border: 1px solid rgba(255, 255, 255, 0.2); /* Borde tenue como cristal */
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
        color: var(--white);
        transition: transform 0.3s ease;
    }

    #hero-content h1 {
        font-family: "Poppins", sans-serif;
        font-size: 2.5rem;
        font-weight: 700;
    }

    #hero-content p {
        font-family: "Poppins", sans-serif;
        font-size: 1.2rem;
    }

    #button-generator {
        background-color: var(--black);
        color: var(--white);
        display: flex;
        align-items: center;
        gap: 0.8rem;
        padding: 0.8rem 1.5rem;
        border-radius: 999px;
        font-family: "Poppins", sans-serif;
        font-size: 1rem;
        cursor: pointer;
        transition:
            background-color 0.3s ease,
            transform 0.3s ease;
    }

    #button-generator:hover {
        background-color: #333;
        transform: scale(1.05);
    }

    #button-generator svg {
        transition: transform 0.3s ease;
    }

    #button-generator:hover svg {
        transform: rotate(180deg);
    }

    @media (max-width: 768px) {
        #hero-content {
            padding: 1.5rem;
        }

        #hero-content h1 {
            font-size: 1.8rem;
        }

        #hero-content p {
            font-size: 1rem;
        }

        #button-generator {
            font-size: 0.9rem;
            padding: 0.6rem 1.2rem;
        }
    }

    @media (max-width: 480px) {
        #hero-content {
            padding: 1rem;
        }

        #hero-content h1 {
            font-size: 1.5rem;
        }

        #hero-content p {
            font-size: 0.9rem;
        }

        #button-generator {
            font-size: 0.8rem;
            padding: 0.5rem 1rem;
        }
    }
</style>

<script>
    const divBienvenida = document.querySelector(".bienvenida");
    const buttonGoToGenerator = document.getElementById("button-generator");

    if (buttonGoToGenerator) {
        buttonGoToGenerator.addEventListener("click", () => {
            window.location.href = "/Rutinas";
        });
    }

    document.addEventListener("DOMContentLoaded", () => {
        const datosUsr = sessionStorage.getItem("usuario");
        if (divBienvenida && datosUsr) {
            const parsedUsr = JSON.parse(datosUsr);
            const firstName = parsedUsr.nombre?.split(" ")[0] || "Usuario";
            divBienvenida.innerHTML = `<h1 style="text-align: center; line-height: 1;">¡Bienvenido/a ${firstName} 💪!</h1>`;
        } else {
            if (divBienvenida) {
                divBienvenida.innerHTML = `<h1 style="text-align: center; line-height: 1;">¡Bienvenido/a, Usuario!</h1>`;
            }
        }
    });
</script>
